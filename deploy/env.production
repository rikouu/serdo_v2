# Serdo API 生产环境配置
# 使用方法: 复制此文件到 api/.env 并修改配置

PORT=4000
API_URL=https://api.your-domain.com

# ⚠️ 必须修改为随机密钥！
# 生成: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_RANDOM_SECRET_IN_PRODUCTION

# ⚠️ 生产环境必须为 true
REDACT_MODE=true

DATA_DIR=./api/data
DB_PATH=./api/data/serdo.db
AUDIT_LOG_FILE=./api/data/audit.log

# 修改为实际前端地址
CORS_ORIGIN=https://your-domain.com

AUTO_BACKUP_INTERVAL=24
BACKUP_RETENTION_DAYS=30
BACKUP_DIR=./backups

