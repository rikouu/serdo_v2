# ================================
# Serdo API 环境配置示例
# ================================

# ========== 必需配置 ==========

# JWT 签名密钥 (必须设置强随机值)
# 生成方法: openssl rand -base64 48
AUTH_SECRET=your_strong_random_secret_key_here_change_me

# CORS 允许的来源 (逗号分隔多个)
# 开发环境: http://localhost:3000
# 生产环境: https://serdo.example.com
CORS_ORIGIN=http://localhost:3000

# ========== 可选配置 ==========

# 服务监听端口
PORT=4000

# 启用 SQLite 数据库 (true/false)
USE_SQLITE=true

# 脱敏模式 (生产环境建议 true，API 不返回明文密码)
REDACT_MODE=true

# 速率限制 (每分钟请求次数)
RATE_LIMIT_MAX=300

# 是否允许明文 SSH 密码登录 (生产环境建议 false)
API_SSH_ALLOW_PASSWORD=true

# ========== 通知配置 (可选) ==========

# Sentry 错误上报 DSN (可选)
# SENTRY_DSN=

# ========== 注意事项 ==========
# 1. 生产环境必须修改 AUTH_SECRET 为强随机值
# 2. 生产环境建议启用 REDACT_MODE=true
# 3. 生产环境建议设置具体的 CORS_ORIGIN
# 4. 定期备份 api/data/ 目录
